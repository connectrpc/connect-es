{
  "mcpServers": {
    "leftenant": {
      "command": "node",
      "args": [
        "${LEFTENANT_MCP_SERVER}"
      ],
      "env": {
        "LEFTENANT_SOCKET": "${LEFTENANT_SOCKET}"
      }
    }
  }
}
