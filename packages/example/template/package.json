{
  "name": "connect-node-example",
  "private": true,
  "type": "module",
  "scripts": {
    "buf:generate": "buf generate",
    "server": "npm run lint && node --loader ts-node/esm server/app.ts",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "esbuild": "^0.16.12",
    "typescript": "^4.8.4"
  }
}
