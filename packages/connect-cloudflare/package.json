{
  "name": "@connectrpc/connect-cloudflare",
  "private": true,
  "scripts": {
    "conformance": "tsc --noEmit && npm run conformance:server",
    "conformance:server": "npx wrangler deploy -c ./conformance/wrangler-server.toml && ./bin/connectconformance --mode server --conf ./conformance/conformance-cloudflare-server.yaml -v ./bin/conformanceserver"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^1.7.2",
    "@connectrpc/connect": "1.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "wrangler": "^3.53.0",
    "tsx": "^4.10.2"
  }
}
